import { describe, it, expect } from "vitest";

describe("Frontend - Auth", () => {
  it("placeholder test passes", () => {
    expect(true).toBe(true);
  });
});

describe("Frontend - Courses", () => {
  it("placeholder test passes", () => {
    expect(true).toBe(true);
  });
});

describe("Frontend - Quizzes", () => {
  it("placeholder test passes", () => {
    expect(true).toBe(true);
  });
});
